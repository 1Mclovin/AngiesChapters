package pluralsight;

public class Proxy {
    /*
    we create a proxy by wrapping up an interface
    proxy resides in between an interface
    we can really only have one proxy within an int5erface
    compile time may have an issue with length
    good utilities built into the java api if and when you need to use it



     */
}
