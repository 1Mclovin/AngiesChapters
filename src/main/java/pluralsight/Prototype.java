package pluralsight;

public class Prototype {
    /*
    A prototype
    this is lowkey like creating an object
    it uses the clone method
    so be sure to implement clonable into our class. when we do use a prototype
    we wont have to use the keyword new but no matter it will still give you a unique instance


     */
}
