package pluralsight;

public class Decorator {
    /*
    Also called a wrapper
    it adds behaviors without affecting others
    a decorator is inheritance based
    it is an alternative to subclassing
    constructor requires instance from hierarchy
    we will eventually always decorate the concrete component
    STREAM IS A DECORATOR
    we are adding more functinality by using more decorators and ect


     */
}
